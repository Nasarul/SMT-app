# SMT App - ডেপ্লয়মেন্ট এবং হোস্টিং গাইড (Deployment & Hosting Guide)

এই ডকুমেন্টটিতে SMT App (Vite + React + Supabase) শেয়ার্ড হোস্টিং-এ লাইভ করার এবং Supabase এর ফ্রি টায়ারের ব্যবহার সম্পর্কে বিস্তারিত তথ্য সেভ করা হলো।

## ১. শেয়ার্ড হোস্টিং-এ (Shared Hosting) ডেপ্লয় করার নিয়ম

যেহেতু প্রজেক্টটি Vite এবং React দিয়ে তৈরি একটি ফ্রন্টএন্ড অ্যাপ্লিকেশন, তাই এটি যেকোনো শেয়ার্ড হোস্টিং (যেমন cPanel) এ সহজেই চালানো যাবে। হোস্টিং শুধু ফ্রন্টএন্ড ফাইলগুলো সার্ভ করবে, আর ব্যাকএন্ড/ডাটাবেস Supabase থেকে চলবে।

### ধাপসমূহ:
1. **প্রজেক্ট বিল্ড করা:** 
   লোকাল কম্পিউটারে প্রজেক্ট ফোল্ডারে টার্মিনাল ওপেন করে নিচের কমান্ডটি রান করুন:
   ```bash
   npm run build
   ```
   *নোট: বিল্ড করার আগে অবশ্যই নিশ্চিত হয়ে নিন যে `.env` ফাইলে Supabase-এর লাইভ URL এবং API Key সঠিকভাবে দেওয়া আছে।*

2. **ফাইল আপলোড করা:** 
   বিল্ড সম্পন্ন হওয়ার পর প্রজেক্টের ভেতরে `dist` নামের একটি নতুন ফোল্ডার তৈরি হবে। আপনার শেয়ার্ড হোস্টিং এর কন্ট্রোল প্যানেলে (cPanel) গিয়ে File Manager ওপেন করুন। এরপর `public_html` ফোল্ডারের ভেতরে `dist` ফোল্ডারের **ভেতরের** সমস্ত ফাইল (index.html, assets ফোল্ডার ইত্যাদি) আপলোড করে দিন।

3. **রাউটিং ফিক্স করা (.htaccess):** 
   React Single Page Application (SPA) হওয়ায় সরাসরি কোনো সাব-পেইজে (যেমন `/dashboard`) ভিজিট করলে 404 error আসতে পারে। এটি সমাধান করতে `public_html` ফোল্ডারে `.htaccess` নামে একটি ফাইল তৈরি করে নিচের কোডটুকু সেভ করুন:

   ```apache
   <IfModule mod_rewrite.c>
     RewriteEngine On
     RewriteBase /
     RewriteRule ^index\.html$ - [L]
     RewriteCond %{REQUEST_FILENAME} !-f
     RewriteCond %{REQUEST_FILENAME} !-d
     RewriteRule . /index.html [L]
   </IfModule>
   ```

---

## ২. Supabase ফ্রি টায়ার (Free Tier) এর লিমিটেশন

আপনাকে এখনই Supabase প্রো (Pro Plan) কিনতে হবে না। ফ্রি টায়ার ব্যবহার করেই অ্যাপটি লাইভ করতে এবং খুব ভালোভাবে চালাতে পারবেন। ফ্রি প্ল্যানে যা যা থাকছে:

* **ডাটাবেস স্টোরেজ:** 500 MB (কয়েক লক্ষ টেক্সট রেকর্ড অনায়াসেই রাখা যাবে)।
* **ফাইল স্টোরেজ:** 1 GB (ইউজারদের ছবি বা পিডিএফ সেভ করে রাখার জন্য)।
* **মান্থলি একটিভ ইউজার:** প্রতি মাসে 50,000 জন ইউজার লগিন/সাইনআপ করতে পারবে। 
* **ব্যান্ডউইথ:** ডাটাবেস এবং স্টোরেজের জন্য মাসে 5 GB পর্যন্ত।

### কখন প্রো (Pro) প্ল্যান ($25/month) কেনার প্রয়োজন হতে পারে?

1. **ডেটা লিমিট পার হলে:** যখন অ্যাপের ডেটা বা ফাইলের সাইজ ফ্রি লিমিট (500 MB ডাটাবেস বা 1 GB স্টোরেজ) পার হয়ে যাবে।
2. **প্রজেক্ট পজ (Pause) ইস্যু:** Supabase-এর ফ্রি টায়ারে, যদি ডাটাবেসে টানা ৭ দিন কোনো রিকোয়েস্ট না আসে (কেউ অ্যাপ ব্যবহার না করে), তবে সার্ভার রিসোর্স বাঁচানোর জন্য প্রজেক্টটি সাময়িকভাবে 'Pause' হয়ে যায়। তখন ড্যাশবোর্ডে গিয়ে আবার রিস্টার্ট করতে হয়। অ্যাপটি নিয়মিত ব্যবহার হলে এটি পজ হবে না। তবে প্রো প্ল্যানে প্রজেক্ট কখনো পজ হয় না।

*শুরু করার জন্য ফ্রি প্ল্যানই সবচেয়ে ভালো। পরবর্তীতে ডেটা এবং ব্যবহারকারী বাড়লে যেকোনো সময় প্রো প্ল্যানে আপগ্রেড করে নেওয়া যাবে।*
